<?xml version="1.0" encoding="UTF-8"?>
<!--
  PRODUCTION manifest.

  The pane is served by `dvload serve` on loopback, not from a public origin,
  so SourceLocation points at localhost — same as manifest.xml. That is the
  point of the design: the UI and its tokens both come from a process running
  as the signed-in user, which is what removes the Entra admin-consent
  requirement. See docs/AUTH-NOTES.md.

  What still differs from manifest.xml is identity and chrome: a stable Id,
  real support/icon URLs. Sideload this one; manifest.xml carries a
  placeholder GUID and is for local iteration.

  The port must match `dvload serve` (default 44321). If you change one,
  change the other.
-->
<OfficeApp
  xmlns="http://schemas.microsoft.com/office/appforoffice/1.1"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:bt="http://schemas.microsoft.com/office/officeappbasictypes/1.0"
  xmlns:ov="http://schemas.microsoft.com/office/taskpaneappversionoverrides"
  xsi:type="TaskPaneApp">

  <Id>a0f4f5da-47e0-47ec-80a1-1919128f464a</Id>
  <!-- The Office manifest version is independent of the product version;
       the validator rejects anything below 1.0. -->
  <Version>1.0.0.0</Version>
  <ProviderName>dvload</ProviderName>
  <DefaultLocale>en-US</DefaultLocale>
  <DisplayName DefaultValue="Dataverse Load"/>
  <Description DefaultValue="Map an Excel table to a Dataverse entity and load via the OData Web API."/>
  <!-- Schema order matters here: IconUrl before SupportUrl. -->
  <IconUrl DefaultValue="https://localhost:44321/assets/icon-32.png"/>
  <HighResolutionIconUrl DefaultValue="https://localhost:44321/assets/icon-80.png"/>
  <SupportUrl DefaultValue="https://github.com/PowerPlatformProfessor/dvload/issues"/>

  <!--
    No AppDomains entry: the pane never navigates off its own origin. The
    login.microsoftonline.com entry that used to live here existed for the
    MSAL.js popup, which no longer happens — sign-in opens a browser from the
    sidecar process instead.
  -->

  <Hosts>
    <Host Name="Workbook"/>
  </Hosts>

  <DefaultSettings>
    <SourceLocation DefaultValue="https://localhost:44321/taskpane.html"/>
  </DefaultSettings>

  <Permissions>ReadWriteDocument</Permissions>

  <VersionOverrides xmlns="http://schemas.microsoft.com/office/taskpaneappversionoverrides" xsi:type="VersionOverridesV1_0">
    <Hosts>
      <Host xsi:type="Workbook">
        <DesktopFormFactor>
          <GetStarted>
            <Title resid="GetStarted.Title"/>
            <Description resid="GetStarted.Description"/>
            <LearnMoreUrl resid="GetStarted.LearnMoreUrl"/>
          </GetStarted>
          <ExtensionPoint xsi:type="PrimaryCommandSurface">
            <OfficeTab id="TabHome">
              <Group id="DataverseLoad.Group">
                <Label resid="GroupLabel"/>
                <Icon>
                  <bt:Image size="16" resid="Icon.16x16"/>
                  <bt:Image size="32" resid="Icon.32x32"/>
                  <bt:Image size="80" resid="Icon.80x80"/>
                </Icon>
                <Control xsi:type="Button" id="DataverseLoad.OpenPane">
                  <Label resid="ButtonLabel"/>
                  <Supertip>
                    <Title resid="ButtonLabel"/>
                    <Description resid="ButtonTooltip"/>
                  </Supertip>
                  <Icon>
                    <bt:Image size="16" resid="Icon.16x16"/>
                    <bt:Image size="32" resid="Icon.32x32"/>
                    <bt:Image size="80" resid="Icon.80x80"/>
                  </Icon>
                  <Action xsi:type="ShowTaskpane">
                    <TaskpaneId>ButtonId1</TaskpaneId>
                    <SourceLocation resid="Taskpane.Url"/>
                  </Action>
                </Control>
              </Group>
            </OfficeTab>
          </ExtensionPoint>
        </DesktopFormFactor>
      </Host>
    </Hosts>
    <Resources>
      <bt:Images>
        <bt:Image id="Icon.16x16" DefaultValue="https://localhost:44321/assets/icon-16.png"/>
        <bt:Image id="Icon.32x32" DefaultValue="https://localhost:44321/assets/icon-32.png"/>
        <bt:Image id="Icon.80x80" DefaultValue="https://localhost:44321/assets/icon-80.png"/>
      </bt:Images>
      <bt:Urls>
        <bt:Url id="GetStarted.LearnMoreUrl" DefaultValue="https://github.com/PowerPlatformProfessor/dvload#readme"/>
        <bt:Url id="Taskpane.Url" DefaultValue="https://localhost:44321/taskpane.html"/>
      </bt:Urls>
      <bt:ShortStrings>
        <bt:String id="GetStarted.Title" DefaultValue="Dataverse Load"/>
        <bt:String id="GroupLabel" DefaultValue="Dataverse"/>
        <bt:String id="ButtonLabel" DefaultValue="Load to Dataverse"/>
      </bt:ShortStrings>
      <bt:LongStrings>
        <bt:String id="GetStarted.Description" DefaultValue="Map and load an Excel table to a Dataverse entity."/>
        <bt:String id="ButtonTooltip" DefaultValue="Open the Dataverse Load pane."/>
      </bt:LongStrings>
    </Resources>
  </VersionOverrides>
</OfficeApp>
