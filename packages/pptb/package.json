{
  "name": "@dvload/pptb",
  "version": "0.2.0",
  "description": "Load Excel / Power Query output into Dataverse from inside the Power Platform ToolBox: map columns, resolve lookups, upsert, sync.",
  "private": true,
  "scripts": {
    "build": "webpack --mode production",
    "clean": "rimraf dist",
    "watch": "webpack --mode development --watch"
  },
  "dependencies": {
    "@dvload/addin": "0.2.0",
    "@dvload/core": "0.2.0"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2",
    "html-webpack-plugin": "^5.6.0",
    "ts-loader": "^9.5.1",
    "webpack": "^5.110.3",
    "webpack-cli": "^5.1.4"
  }
}
